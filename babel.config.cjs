module.exports = {
  presets: [
    '@babel/preset-env',      // For modern JS syntax
    '@babel/preset-react',    // For JSX
    '@babel/preset-typescript', // For TypeScript support
  ],
  plugins: [
    // Optional plugins for other transformations, if needed
  ],
};
